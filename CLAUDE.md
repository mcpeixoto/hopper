# hopper — Project Instructions

Internal tool repo. Not deployed on the Peixotolabs fleet (`deploy: none` in registry).

## Before declaring done

Follow existing repo conventions. Never commit automatically unless the user asks.

## References

- Fleet registry: `/root/AdminPanel/registry.yml`
- Server infra: `/root/SERVER_INFRA.md`, `/root/CLAUDE.md`
